{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "build:sample": "rm -rf ./sample && rm -rf ./react-project/sample/build && npm run build --prefix react-project/sample && mkdir sample && mv react-project/sample/build/* ./sample"
  },
  "devDependencies": {
    "eslint": "^8.51.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.0.3"
  },
  "dependencies": {
    "eslint-plugin-react-refresh": "^0.4.3",
    "eslint-plugin-unused-imports": "^3.0.0"
  }
}
